{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch index.html in Firefox",
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "file": "${workspaceFolder}/index.html",
      "reloadOnChange": {
        "watch": [
          "${workspaceFolder}/index.html",
          "${workspaceFolder}/src/*",
          "${workspaceFolder}/src/**/*"
        ]
      }
    }
  ]
}
